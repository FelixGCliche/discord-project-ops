{
  "name": "discord-project-ops",
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "check": "tsc --noEmit && oxlint && oxfmt",
    "check:type": "tsc --noEmit",
    "check:lint": "oxlint",
    "check:format": "oxfmt --check",
    "fix:lint": "oxlint --fix",
    "fix:format": "oxfmt"
  },
  "dependencies": {
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
