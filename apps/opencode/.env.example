OPENCODE_GO_API_KEY=<YOUR_OPENCODE_API_KEY>

# Optional -- defaults shown. Only override for local runs; the Docker image
# sets these itself (see Dockerfile).
OPENCODE_SERVER_HOSTNAME=0.0.0.0
OPENCODE_SERVER_PORT=4096
