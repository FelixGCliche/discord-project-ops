{
  "name": "opencode",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "check": "tsc --noEmit && oxlint && oxfmt",
    "check:type": "tsc --noEmit",
    "check:lint": "oxlint",
    "check:format": "oxfmt --check",
    "fix:lint": "oxlint --fix",
    "fix:format": "oxfmt"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.17.15",
    "core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
